# RAG Knowledge MCP Server Configuration
# Copy this file to .env and customize as needed

# Knowledge base source directory (input for ingestion)
# Relative paths are resolved from the project directory (where config.py is)
RAG_KNOWLEDGE_DIR=./knowledge-base

# Vector database directory (output/storage)
# Relative paths are resolved from the project directory (where config.py is)
RAG_PERSIST_DIR=./chroma_db

# Collection name in the vector database
RAG_COLLECTION=knowledge_base

# Sentence Transformers embedding model
# Options:
#   - all-MiniLM-L6-v2 (default, fast, 384 dimensions)
#   - all-mpnet-base-v2 (higher quality, 768 dimensions)
#   - multi-qa-mpnet-base-dot-v1 (optimized for Q&A)
RAG_EMBEDDING_MODEL=all-MiniLM-L6-v2

# Backend implementation (module.ClassName format)
# Default: chroma_backend.RagBackend
# This allows swapping vector database implementations without changing code.
# Examples:
#   RAG_BACKEND_CLASS=chroma_backend.RagBackend (default ChromaDB backend)
#   RAG_BACKEND_CLASS=pinecone_backend.PineconeBackend (if you implement Pinecone)
#   RAG_BACKEND_CLASS=my_custom_backend.MyBackend (custom implementation)
RAG_BACKEND_CLASS=chroma_backend.RagBackend

# Test environment configuration (for pytest tests)
# Tests use separate database and knowledge base to avoid corrupting production data
TEST_RAG_KNOWLEDGE_DIR=./test-knowledge-base
TEST_RAG_PERSIST_DIR=./test_chroma_db
TEST_RAG_COLLECTION=test_knowledge_base
TEST_RAG_EMBEDDING_MODEL=all-MiniLM-L6-v2
TEST_RAG_BACKEND_CLASS=chroma_backend.RagBackend
